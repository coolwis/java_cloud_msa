spring:
  cloud:
    gateway:
      routes:
        - id: composite
          uri: lb://composite
          predicates:
            - Path=/composite/**
eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka:8761/eureka/